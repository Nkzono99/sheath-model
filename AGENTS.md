# AGENTS.md

## 対象と責務

- 独立したシース物理モデルを扱う。BEACH など利用側の状態管理や結合用インタフェースは取り込まない。
- Fortran の公開窓口は `src/sheath_model.f90`、Python 実装は `sheath_model/`。両者の分布関数・単位・物理条件を揃える。
- モデルの仮定は `docs/kinetic-model.md`、公開 API は `docs/fortran-api.md` を参照する。

## API 変更

- **互換性のためだけに旧 API、別名、未使用引数・出力、変換層を残さない。** 公開済みの API も対象とする。ユーザーが明示的に互換性を求めた場合だけ例外とする。
- 名前と実際の意味がずれた API は改名または削除し、呼び出し側・利用例・テスト・文書を同時に更新する。旧 API の維持だけを検証するテストも残さない。
- 入力・出力・単位・固定量と未知量を明示し、シースモデルに必要な範囲で API を設計する。

## 物理と検証

- 代数方程式の収束と物理プロファイルの存在を区別する。数値的な根の選択を安定性の保証と呼ばない。
- 分布関数、密度、流束の整合性は独立な積分や保存則で検証する。
- 変更に応じて `fpm test` と `python -m unittest discover -s tests -v` を実行する。
- KUDPC 上のビルド・実行・テストは KUDPC plugin の手順に従い、ログインノードで計算を実行しない。
